import React from 'react';
import {Text, View} from 'react-native';

const settings = () => {
  return (
      <View>
        <Text>Create a component for the settings</Text>
      </View>
    );
};

export default settings;
